import React from "react";

// Should contain DELETE fetch and map method, will replace ConcertDisplay file??? Likely will not have both files.  Edit button will be on this file as well, but edit functinality will be on ConcertEdit file.

class ConcertTable extends React.Component{


    // map method and likely <ul> of shows and not a table
    render(){
        return(
            <div>
                <h1>ConcertTable Component</h1>
            </div>
        )
    }
}

export default ConcertTable;